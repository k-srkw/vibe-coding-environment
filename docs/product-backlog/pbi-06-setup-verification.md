# PBI-6: 環境構築完了の自動検証

## ステータス: 完了

## 依存

- [PBI-5: GitHub Actions 環境構築自動化](pbi-05-github-actions-setup.md)

## 説明

環境構築が正しく完了したことを検証するヘルスチェックの仕組みを作成する。検証結果をわかりやすくレポートする。

## 受け入れ条件

- [x] 以下の項目を自動検証する:
  - 必要なファイル（CLAUDE.md, package.json, feature ファイル等）が存在する
  - 依存パッケージがインストールされている
  - Cucumber テストが実行可能である
  - ビルドが成功する
- [x] 検証結果が GitHub Actions のジョブサマリーに表示される
- [x] 全チェック項目がパスした場合に「環境構築完了」と判定される
- [x] 1 項目でも失敗した場合にエラー内容が明示される

## 受け入れ条件の検証方法

| 受け入れ条件 | 検証方法 | 対応するシナリオ |
|-------------|---------|----------------|
| 必要なファイル（CLAUDE.md, package.json, feature ファイル等）が存在する | スクリプト内容の静的検証（自動テスト） | 検証スクリプトが skeleton に存在する / 必要なファイルの存在を自動検証する |
| 依存パッケージがインストールされている | スクリプト内容の静的検証（自動テスト） | 依存パッケージのインストール状態を自動検証する |
| Cucumber テストが実行可能である | スクリプト内容の静的検証（自動テスト） | Cucumber テストの実行可能性を自動検証する |
| ビルドが成功する | スクリプト内容の静的検証（自動テスト） | ビルドの成功を自動検証する |
| 検証結果が GitHub Actions のジョブサマリーに表示される | YAML 構造の静的検証（自動テスト） | 検証結果が GitHub Actions のジョブサマリーに表示される |
| 全チェック項目がパスした場合に「環境構築完了」と判定される | スクリプト実行による動的検証（自動テスト） | 全チェック項目がパスした場合に環境構築完了と判定される |
| 1 項目でも失敗した場合にエラー内容が明示される | スクリプト実行による動的検証（自動テスト） | 1 項目でも失敗した場合にエラー内容が明示される |

## 想定する成果物

- 検証スクリプト
- `setup.yml` への組み込み

## 完了証跡

- テスト実行結果: 30 tests (29 passed, 1 skipped), 8 シナリオ全て Green
- 確認日時: 2026-07-11
- 備考: PBI-6 の 8 シナリオすべて passed。skipped は既存の `@github` タグ付きテスト（PBI-2、GITHUB_TOKEN 未設定時スキップ）。既存テストのリグレッションなし。リファクタリングにより workflow 型定義と skeleton 一時ディレクトリヘルパーを共通化。
