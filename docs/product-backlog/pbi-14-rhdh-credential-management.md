# PBI-14: RHDH Local の認証情報管理を環境変数ベースに統一

## ステータス: 未着手

## 依存

- なし

## 説明

現在、RHDH Local の GitHub 認証情報が `app-config.local.yaml` にハードコードされる運用になっている。`rhdh-local/` は `.gitignore` で除外されているが、`rhdh-local/` 自体の git 履歴に認証情報が残るリスクがある。環境変数または Docker secret ベースの認証情報管理に移行する。

## 受け入れ条件

- [ ] GitHub PAT が `app-config.local.yaml` にハードコードされていない
- [ ] 認証情報が環境変数経由で RHDH に渡される仕組みが構築されている
- [ ] README に環境変数ベースの設定手順が記載されている
- [ ] 全テストが Green である

## 受け入れ条件の検証方法

| 受け入れ条件 | 検証方法 | 対応するシナリオ |
|-------------|---------|----------------|
| <!-- 着手時に記入 --> | | |

## 想定する成果物

- RHDH Local の設定ファイル更新
- README の設定手順更新

## 完了証跡

<!-- PBI 完了時に記入。このセクションが空の状態で「完了」にしてはならない -->
- テスト実行結果:
- 確認日時:
- 備考:
